import { Request, Response } from 'express';
import { GoogleCalendarHandler } from '../Handlers/GoogleCalendarHandler';
import { ResponseProcessor } from '../Services/ResponseProcessor';

export class GoogleCalendarController {
    public static async createEvent(req: Request, res: Response) {
        try {

        }
        catch(err: any) {

        }
    }

    public static async editEvent(req: Request, res: Response) {
        try {

        }
        catch(err: any) {
            
        }
    }

    public static async deleteEvent(req: Request, res: Response) {
        try {

        }
        catch(err: any) {
            
        }
    }
}